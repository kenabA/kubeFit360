import { TriangleIcon } from "lucide-react";

export default function Triangle() {
  return (
    <TriangleIcon className="stroke-none fill-[hsl(var(--primary))] absolute left-0 -rotate-90 top-1/2 -translate-y-1/2 -translate-x-[45%]" />
  );
}
